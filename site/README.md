# Kosmos website assets

This directory stores the Lightbox badge and social preview used by the React
website in `website/`. GitHub Pages builds that Vite app and publishes
`website-dist/`.

The site stays private with the organization repository until public release.
