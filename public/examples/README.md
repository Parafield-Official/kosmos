# Kosmos examples

The `proof/on_vs_in.*` fixture contains a short, synthetic speech recording
whose transcript deliberately says `in` where the manuscript says `on`. It is
test audio, not generated audiobook output; it exists so the proof alignment
pipeline can be exercised without uploading a real manuscript or voice.
