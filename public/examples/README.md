# Booth Desk examples

The shipped Journey A example will contain a short manuscript and recording
with a deliberate `on` → `in` substitution. It is kept small so a new user can
complete the first proof locally in a few minutes.

