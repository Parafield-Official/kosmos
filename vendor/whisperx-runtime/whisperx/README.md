# Generated WhisperX runtime

Release CI replaces this placeholder with the platform-specific PyInstaller
`onedir` build. The directory is packaged under `Resources/whisperx` so Kosmos
can use faster-whisper/CTranslate2 on every supported installation without a
separate Python setup.
